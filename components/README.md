# ESP-IDF Components Directory

This directory contains custom ESP-IDF components for the BalloonInteliPump project.
Subdirectories placed here containing a `CMakeLists.txt` will automatically be registered by ESP-IDF build system.
